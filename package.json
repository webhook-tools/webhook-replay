{
  "name": "webhook-replay",
  "version": "1.0.0",
  "description": "Replay webhook deliveries against a handler to detect duplicate side effects.",
  "bin": {
    "webhook-replay": "bin/webhook-replay.js"
  },
  "scripts": {
    "test": "echo \"No tests yet\" && exit 0",
    "ci:replay": "webhook-replay ./examples/handler.js",
    "debug:replay": "webhook-replay debug ./examples/handler.js --trace"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/webhook-tools/webhook-replay.git"
  },
  "keywords": ["webhook", "replay", "idempotency", "stripe", "testing", "ci"],
  "author": "",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/webhook-tools/webhook-replay/issues"
  },
  "homepage": "https://github.com/webhook-tools/webhook-replay#readme"
}
